table:
  name: notification_master
  schema: public
object_relationships:
  - name: users
    using:
      manual_configuration:
        column_mapping:
          user_id: id
        insertion_order: null
        remote_table:
          name: users
          schema: auth
